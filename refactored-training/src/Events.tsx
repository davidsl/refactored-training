import styles from './Events.module.css';

function Events() {
  return (
    <div className={styles.eventsContainer}>
      <h2>Events</h2>
      <p>Find local events happening near you, displayed on an interactive map.</p>
    </div>
  );
}

export default Events;
